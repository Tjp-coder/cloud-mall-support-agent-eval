# Codex 启动提示词（v3 · 2026-08-22 顺延版）

> 用法：新开 Codex 会话，把下方代码块整段作为第一条消息粘贴。
> 已内置两处修正：① 文档口径 =“自己整理编写的模拟文档”，禁用“上家公司真实文档”表述；② 指标门槛 = 基线对比参考值，未达标记录原因即完成。
> 若本文件与 `完整计划.md` 冲突，以 `完整计划.md` 为准。

---

```text
你在协助我完成一个 15 天个人项目：算力云商城客服问答 Agent 测试专项。动手之前，先记住三个定位：

【1. 项目定位：SUT 极简，测试资产为主】
Agent 只是被测系统（SUT），不是项目主角。写 Agent 的时间封顶 3 天（D0–D3），其余 80% 时间产出的东西是：测试方案、测试用例、评测框架、缺陷单、对比报告。我的求职目标是测试岗，面试官要看的是测试方案设计能力，不是 Agent 搭得牛不牛。

【2. 业务口径（红线，所有产出物统一，不许改）】
本项目的知识库是我基于自己之前在算力云商城业务上的理解、自己整理编写的一套模拟业务文档——不是“上家公司真实业务文档”，也不是内部系统导出的。README、报告、面试材料里凡涉及文档来源，统一使用这句：
“这是我基于自己之前在算力云商城业务上的理解，自己整理编写的一套模拟业务文档，做的一个 Agent 测试方法论实践项目。”
禁用表述：“上家公司真实业务文档”“公司内部资料”“生产数据”“内部系统导出”。文档只保留业务逻辑层面的事实，不含任何公司内部数据、代码或资产。

【3. 指标口径（红线，不许改）】
所有质量指标门槛（faithfulness≥0.9、answer_relevancy≥0.8、context_precision≥0.85、context_recall≥0.80、人机一致率≥85% 等）都是基线对比参考值，不是及格线。实测未达标 = 一个数据点，不是失败：标准动作是记录原因、写归因分析、作为面试素材，该项即算完成。严禁为了凑分数反复调参消耗工期。调优实验封顶：检索 5 组 + prompt 2 组，跑完即止。面试话术示例：“faithfulness 基线 0.72，加 query rewrite 后到 0.85，还没到 0.9 参考线，分析原因是 XX”。
唯一例外：安全对抗攻击成功率必须 0/7——攻击成功属于缺陷，修复后复验到 0/7。

【4. 技术栈与 SUT（极简实现，不追求生产级）】
- SUT：RAG 客服问答 Agent（检索→生成），CLI + FastAPI（POST /chat、GET /health）
- 技术栈：Python / LangChain / Chroma / DashScope text-embedding-v3 / 千问 OpenAI 兼容接口 / RAGAS / pytest
- 3 个 mock 业务工具：query_order / query_invoice / query_voucher_balance，服务端支持正常模式 + 故障注入（429 / 500 / timeout）
- 每次调用埋点：latency、input/output token、question、answer、contexts（trace 落 jsonl）

【5. 测试集（核心资产，最重要）】
- 60 条：30 正常业务问答 + 10 边界 + 8 库外拒答 + 5 模糊澄清 + 7 安全对抗，7 个业务模块全覆盖，高风险模块（开票/返佣/告警）各 ≥5 条
- 每条字段：id / module / type / difficulty / question / expected_action / reference_answer / golden_contexts / tools_allowed / assertion
- 检索类 40 条全部人工标 golden_contexts——这是硬要求：RAGAS 的 context_recall 没有 golden_contexts 会算成 NaN，报告直接开天窗。拒答/澄清/工具类允许为空
- 冻结后改用例必须升版本、记 hash

【6. 评测与校准】
- RAGAS 四指标：faithfulness / answer_relevancy / context_precision / context_recall；先 5 条冒烟确认非 NaN，再跑全量
- 30 条裁判校准集人工打分，算一致率 + Cohen's kappa；不达参考值调裁判 prompt 最多 1 轮，结果照实写

【7. 排期（D0 = 2026-08-16 周日，已完成；D1 = 2026-08-22 周六重新开工；新到期线 = 2026-09-05 周六；每天开工先核对系统日期）】
D0 8/16 立项与测试方案 v0（已完成）→ D1 8/22 入库 + 检索 smoke → D2 8/23 问答链 + 埋点 → D3 8/24 FastAPI + mock 工具 + 故障注入 → D4 8/25 60 条初稿（人工标注专注日）→ D5 8/26 评审冻结 → D6 8/27 RAGAS 基线 → D7 8/28 检索调优 → D8 8/29 prompt 调优 + 冻结 v1 → D9 8/30 裁判校准（人工打分专注日）+ 缺陷整理 → D10 8/31 工具/容错 → D11 9/1 安全对抗 → D12 9/2 结构化输出 + 多轮 → D13 9/3 延迟成本汇总 → D14 9/4 总报告 + 追溯 + README → D15 9/5 回归 + 10 题验收。
注意：8/17–8/21 因面试准备暂停，不计入虚假项目工时。D1（8/22 周六）、D2（8/23 周日）、D8（8/29 周六）、D9（8/30 周日）、D15（9/5 周六）落在周末，按计划执行。

【8. 每天收工动作】
更新自检清单真实数字；在完整计划“变更记录”写一行（做了什么、砍了什么、为什么）；新想法只进 backlog。INTERVIEW_QA_MAP.md（骨架即 doc/算力云商城Agent测试-自检计划清单.md）是面试前唯一复习文件，10 道题全部要有真实数字。

【9. 治理铁律】
任何新增测试维度先写进可选清单并标半天成本，不口头加塞、不当日加塞；本计划已定稿，不再横向对比新方案。D4–D5 标注、D9 裁判打分是纯人工专注时段，AI 无法代做——你的任务是提前把工具和脚手架备好，别在人工日塞杂活。

执行方式：从 D0 开始。每天开工先确认系统日期，再汇报当天任务、验收标准和产出物；收工汇报真实数字。有不确定的地方先问我，不要自行扩大范围。
```
